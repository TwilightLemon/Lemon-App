<Page x:Class="LemonApp.Views.Pages.AlbumListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:base="clr-namespace:LemonApp.Common.UIBases;assembly=LemonApp.Common"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:LemonApp.Views.Pages"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:uc="clr-namespace:LemonApp.Views.UserControls"
      Title="AlbumListPage"
      d:DesignHeight="450"
      d:DesignWidth="800"
      Foreground="{DynamicResource ForeColor}"
      mc:Ignorable="d">

    <base:ScrollViewer ScrollChanged="ScrollViewer_ScrollChanged" Style="{StaticResource SimpleVerticalScrollViewerStyle}">
        <StackPanel>
            <TextBlock Margin="24,12"
                       VerticalAlignment="Top"
                       FontSize="24"
                       FontWeight="Bold"
                       Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Page}}" />
            <uc:AlbumItemView x:Name="viewer" />
        </StackPanel>
    </base:ScrollViewer>
</Page>
